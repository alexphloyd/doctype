import { DocumentsList } from '~/entities/document/ui/list';

export const Home = () => {
    return (
        <main className="flex w-full px-[1.5%] py-2">
            <DocumentsList />
        </main>
    );
};
